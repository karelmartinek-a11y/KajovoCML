# Project phase

KajovoCML is `PRE_PRODUCTION_TESTING` as of 2026-08-09. The approved target is the internal AI generation model in `SSOT_CURRENT.md`. Breaking retirement of the former external programmer/integration-token product flow is intentional.

Production readiness still requires the normal supported-runtime install/build/typecheck/test evidence plus deployment-environment database, systemd/nginx/TLS and browser validation. No GitHub CI/deploy success is a completion gate for internally generated components.
