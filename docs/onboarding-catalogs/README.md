# Catalog status

`external-api-1.0.json` remains active for the managed external API feature. Legacy `onboarding-1.1.json` and `repository-component-1.1.json` are retained only as historical/forensic compatibility artifacts; they are not served as the creation contract for internally generated components and do not authorize an integration-token/programmer handoff. New generated components use `docs/SSOT_CURRENT.md` and the canonical component manifest schema.
